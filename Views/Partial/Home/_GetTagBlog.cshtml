@model IEnumerable<TD.Models.Views.TagBlogViewModel>
<div class="tags-container">
    <ul class="tags">
        @foreach (var item in Model)
        {
            <li>
                <a href="/Blog/@item.Name">@item.FullName <span>@item.BlogCount</span></a>
            </li>

        }
    </ul>
</div>