@model IEnumerable<TD.Models.Views.VersionFeatureAppView>
<div>
    <h4 class="form-title">Chức năng</h4>
    <div class="table">
        <div class="table-row table-row-header">

            <span class="table-cell">
                Chức năng
                <a href="/admin/products/VersionFeatureAppsAdd?AppId=@ViewBag.AppId&Version=@(ViewBag.Version)" class="btn left" title="Thêm mới">
                    Thêm mới
                </a>
            </span>
            <span class="table-cell">
                Chức năng
            </span>

        </div>
        @if (Model != null && Model.Any())
            {
                foreach (var item in Model)
                {
                <div class="table-row">

                    <span class="table-cell">
                        <a href="/admin/products/VersionAppFeaturesEdit?AppId=@item.AppId&Version=@(item.Version)&FeatureAppId=@item.FeatureAppId" class="product-slide-link" title="Click vào đây để xem chi tiết">
                            @item.FeatureAppName
                        </a>
                    </span>
                    <span class="table-cell">
                        @item.Content
                    </span>

                </div>
            }
        }
        else
        {
            <div class="table-row">
                <span class="table-cell">Chưa có dữ liệu</span>
            </div>
        }
    </div>
</div>
